language: php

sudo: true

services:
  - mongodb

php:
  - 7.0

before_script:
  - echo "extension = mongodb.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - phpenv config-rm xdebug.ini
  # - composer --prefer-source --no-interaction --dev update

script: phpunit --coverage-text --verbose
